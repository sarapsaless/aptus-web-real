# Renomeie ou copie para secrets.toml em DESENVOLVIMENTO LOCAL (nunca faça commit de secrets.toml).
# No Streamlit Cloud: Project settings → Secrets — cole as mesmas chaves em formato TOML.

# Connection string PostgreSQL (Supabase: Settings → Database → URI; use pooler porta 6543 se precisar)
DB_URL = "postgresql://USER:SENHA@HOST:6543/postgres"
