# --- Segredos (nunca commit) ---
.streamlit/secrets.toml
.env
.env.*

# --- Ambiente Python ---
.venv/
venv/
__pycache__/
*.pyc

# --- Dados locais / cópias / fila offline ---
backups/
offline_queue.json
*.log
audit_sync.log
aptus_errors.log

# --- Sistema desktop / empacotamento (não é a app web) ---
*.spec
*.iss
build_aptus.bat
build_dist.ps1
empacotar_entrega.ps1
build/
dist/

# --- Pastas que não fazem parte do Streamlit mínimo ---
ativos/
construir/
*saida*
*instalador*
